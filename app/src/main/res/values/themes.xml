<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Plain framework theme, not Material Components — the app is pure
         Compose now (no XML-based Material widgets), and Compose supplies
         its own Material3 theming via MaterialTheme in AuraOrbitTheme.kt.
         This just needs to be a valid no-action-bar window theme. -->
    <style name="Theme.AuraOrbit" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
