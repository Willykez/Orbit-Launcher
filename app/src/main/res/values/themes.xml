<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Single app theme — Material 3, follows system dark mode. Home screen
         and drawer are always dark regardless (see AuraOrbitTheme.kt), but
         the window theme itself still needs a valid Material3 parent. -->
    <style name="Theme.AuraOrbit" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
